// old_javascript

function getName(name) {
  return "Hello " + name + " !";
}

console.log(getName("Lux Academy"));

// es6

const myName = (name) => {
  return `Hello ${name} !`;
};

console.log(myName("Lux Academy"));
